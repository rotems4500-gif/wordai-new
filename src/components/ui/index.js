// Shared UI component library (Wave A). RTL-aware, accessible, token-driven.
// Import: import { Button, Input, Modal } from './components/ui';
export { default as Button } from './Button';
export { default as Input, Input as TextInput, TextArea, Select } from './Input';
export { default as Modal } from './Modal';
