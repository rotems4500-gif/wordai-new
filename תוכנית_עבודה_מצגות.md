# תוכנית עבודה: פיצ'ר יצירת/שיפור מצגות

> מסמך להדבקה ב‑Claude Code. עובד לפי שלבים, כל שלב עם מטרה, קבצים, משימות וקריטריון "סיום".

---

## 0. הקשר וההחלטה הקריטית (לקרוא קודם)

הפיצ'ר עושה שני דברים:
1. **Create from scratch** — המשתמש נותן נושא / טקסט גולמי → מצגת מלאה.
2. **Improve existing** — המשתמש נותן מצגת קיימת → גרסה משופרת.

**הלב של הפיצ'ר הוא לא יצירת תמונות ולא פריימוורק עיצוב כבד.** הלב הוא:
`טקסט גולמי → (Gemini) → JSON schema של שקפים → (Renderer + Theme) → קובץ .pptx`

### החלטה אחת לפני שמתחילים — פורמט הרינדור
**כלל אצבע: תשתמש באותה טכנולוגיה שבה הפיצ'ר של מסמכי הוורד כבר מייצר .docx.**
- אם יצירת ה‑Word היא **JS/TS** (למשל ספריית `docx`) → רנדר ל‑pptx עם **`pptxgenjs`**.
- אם יצירת ה‑Word היא **Python** (`python-docx`) → רנדר עם **`python-pptx`**.

**משימה ראשונה ל‑Claude Code:** סרוק את הקוד הקיים של יצירת ה‑docx וזהה את הסטאק. דווח לי מה מצאת לפני שאתה ממשיך. כל מה שמתחת לרמת ה‑Renderer (Schema + Gemini + Theme) זהה בשני המסלולים — רק שכבת הרינדור משתנה.

> בהמשך המסמך אני כותב את הדוגמאות ב‑`pptxgenjs` (TS) כברירת מחדל. אם הסטאק פייתון — תרגם את שלב הרינדור ל‑`python-pptx` (המושגים זהים: slide master = layout, placeholders = tokens).

---

## 1. Schema + Types — הבסיס לכל השאר

**מטרה:** להגדיר חוזה (contract) אחד שגם Gemini מייצר וגם ה‑Renderer צורך.

**קובץ:** `src/features/deck/schema.ts`

```ts
type Layout =
  | "title" | "section" | "bullets" | "two-column"
  | "image-text" | "quote" | "chart" | "closing";

interface SlideImage { query: string; position?: "left" | "right" | "full" | "background"; }
interface SlideChart { type: "bar" | "line" | "pie"; labels: string[]; data: number[]; }

interface Slide {
  id: string;
  layout: Layout;
  title?: string;
  subtitle?: string;
  bullets?: string[];      // עד 6, כל אחד עד ~12 מילים
  body?: string;
  image?: SlideImage;      // query לחיפוש stock, לא URL
  icon?: string;           // שם אייקון Lucide
  chart?: SlideChart;
  notes?: string;          // הערות למרצה
}

interface Deck {
  meta: { title: string; subtitle?: string; author?: string;
          theme: string; language: "he" | "en"; aspectRatio: "16:9" | "4:3"; };
  slides: Slide[];
}
```

**קריטריון סיום:** הטיפוסים מקומפלים, יש דוגמת `Deck` תקינה בקובץ בדיקה.

---

## 2. שכבת ה‑Theme — Design Tokens + Layouts

**מטרה:** מערכת עיצוב קלה מבוססת JSON, לא פריימוורק. שתי ערכות נושא לפחות.

**קובץ:** `src/features/deck/themes.ts`

```ts
interface Theme {
  name: string;
  colors: { primary: string; secondary: string; accent: string;
            background: string; text: string; muted: string; };
  fonts: { heading: string; body: string; };
}
```

- צור 2 themes: אחת בהירה/מינימליסטית, אחת כהה/מודרנית.
- שים לב לתמיכת **RTL** בעברית (יישור ימינה, פונט עברי תקין כמו Assistant / Rubik).
- ב‑`pptxgenjs` ה‑layouts ממומשים דרך `defineSlideMaster` — slide master אחד לכל ערך ב‑`Layout`.

**קריטריון סיום:** אפשר לקרוא ל‑`getTheme("dark")` ולקבל אובייקט מלא; כל ה‑8 layouts מוגדרים כ‑masters.

---

## 3. יצירת תוכן עם Gemini (Create from scratch)

**מטרה:** פונקציה שמקבלת טקסט/נושא ומחזירה `Deck` תקין — **JSON בלבד**.

**קובץ:** `src/features/deck/generate.ts`

**System prompt ל‑Gemini (חשוב — זה החלק עם הכי הרבה מינוף):**
```
אתה מעצב מצגות מקצועי. קבל נושא/טקסט והפק מצגת.
החזר JSON תקין בלבד התואם ל‑Deck schema — בלי טקסט מקדים, בלי ```.
כללים:
- 8–14 שקפים. שקף ראשון layout="title", אחרון "closing".
- כל ~3–4 שקפי תוכן הוסף שקף "section" מפריד.
- מקס 6 בולטים לשקף, כל בולט קצר וחד.
- בכל שקף תוכן הצע image.query אנגלי לחיפוש stock, או icon מ‑Lucide.
- מלא notes (הערות מרצה) של 2–3 משפטים לכל שקף.
- שמור עקביות שפה (עברית) ורמת קהל אחידה.
```

**חובה:**
- ולידציה של הפלט מול ה‑schema (Zod או בדיקה ידנית). אם לא תקין — retry פעם אחת עם הודעת תיקון.
- ניקוי ` ```json ` שעוטף תשובות לפני `JSON.parse`.

**קריטריון סיום:** קלט "מצגת על בינה מלאכותית בחינוך" → `Deck` תקין שעובר ולידציה.

---

## 4. ה‑Renderer — Schema → .pptx

**מטרה:** להמיר `Deck` לקובץ מצגת אמיתי.

**קובץ:** `src/features/deck/render.ts`

**עם pptxgenjs:**
1. `new pptxgen()`, הגדר `defineLayout` / aspect ratio.
2. לכל `Layout` קרא ל‑`defineSlideMaster` עם רקע, מיקומי placeholders וצבעים מה‑theme.
3. לכל שקף: `pptx.addSlide({ masterName })` ואז מילוי `addText` / `addImage` / `addChart` לפי ה‑layout.
4. `notes` → `slide.addNotes(...)`.
5. החזר Blob / Buffer להורדה.

**שים לב:**
- RTL: יישור `align: "right"`, `rtlMode: true` היכן שנתמך, פונט עברי.
- אל תיתן ל‑Gemini לייצר קואורדינטות פיקסלים — המיקומים נקבעים ב‑masters בלבד. Gemini נותן רק תוכן וסוג layout.

**קריטריון סיום:** `Deck` לדוגמה → קובץ .pptx שנפתח תקין ב‑PowerPoint, עם themes ו‑layouts נכונים.

---

## 5. Assets — תמונות, אייקונים, גרפים

**מטרה:** ויזואלים בלי לייצר תמונות AI (יקר, לא עקבי).

**קובץ:** `src/features/deck/assets.ts`

- **Stock images:** API של **Unsplash** או **Pexels** (חינם, נדרש API key). `image.query` → תמונה → הזרק ל‑slide.
- **Icons:** **Lucide** (כבר מוכר לך) — מיפוי `icon` name → SVG/PNG.
- **Charts:** מובנה ב‑pptxgenjs (`addChart`), לא צריך ספרייה חיצונית.
- **Fallback:** אם אין תמונה — ליפול חזרה ל‑layout מבוסס אייקון/צבע, לא להישבר.

**קריטריון סיום:** שקף "image-text" מקבל תמונת stock אמיתית; כשל ב‑API נופל ל‑fallback בלי קריסה.

---

## 6. מסלול Improve existing

**מטרה:** לקרוא מצגת קיימת, לחלץ תוכן, ולשפר.

**קובץ:** `src/features/deck/improve.ts`

1. **קריאת ה‑pptx הקיים** → חילוץ טקסט פר‑שקף (ב‑pptxgenjs אין parsing — השתמש ב‑`pptx-parser`/`jszip` לקריאת ה‑XML, או ב‑`python-pptx` אם פייתון).
2. הזן לג'מיני עם הוראת שיפור: "שדרג ניסוח, מבנה, היררכיה, וצמצם עומס טקסט. החזר Deck schema."
3. רנדר מחדש דרך אותו Renderer של שלב 4.

**קריטריון סיום:** העלאת מצגת חלשה → מתקבלת מצגת מובנית יותר עם אותו תוכן מהותי.

---

## 7. אינטגרציה ב‑UI

**מטרה:** לחבר את הפיצ'ר לאפליקציה.

- מסך קלט: בורר בין "מאפס" (textarea + בורר theme) ל"שיפור" (העלאת קובץ).
- **Preview** לפני הורדה — לפחות רינדור פשוט של ה‑schema (כותרות + בולטים) כדי שהמשתמש יאשר לפני יצירת ה‑pptx.
- כפתור הורדה / שמירה.
- מצבי loading ושגיאה (קריאת Gemini יכולה להיכשל).

**קריטריון סיום:** flow מלא end‑to‑end מתוך ה‑UI, משני המסלולים.

---

## 8. Polish + עמידות

- Rate limiting / retry על קריאות Gemini ו‑Unsplash.
- Cache ל‑schema שנוצר (כמו שעשית עם localStorage בעבר).
- Validation ברורה + הודעות שגיאה למשתמש.
- בדיקה: עברית ארוכה, מצגת בלי תמונות, מצגת עם גרפים.

---

## תלויות מותקנות (מסלול JS)
```
pptxgenjs        # יצירת pptx
zod              # ולידציית schema
jszip            # קריאת pptx קיים (improve)
lucide-react     # אייקונים
# Gemini SDK — כבר אצלך
# Unsplash/Pexels — דרך fetch + API key, בלי SDK
```

## סדר עבודה מומלץ ל‑Claude Code
זהה סטאק (0) → schema (1) → theme (2) → **רנדר שקף בודד hard‑coded (4 חלקי)** → Gemini (3) → חבר 3→4 → assets (5) → improve (6) → UI (7) → polish (8).

> טיפ: אל תחבר את Gemini לרנדרר לפני ששקף בודד hard‑coded מתרנדר יפה. ככה אתה מפריד בעיות תוכן מבעיות עיצוב.
