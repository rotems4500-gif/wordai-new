# `neg-ai-labor` — פרובה שלילית החזירה ראיות · **נסגר 31.7.2026**

**נכתב:** 27.7.2026 · **מכונה:** מכונת הפיתוח החלשה (`C:/Users/rotem`) ·
**קומיט:** `36789b4` · **סטטוס:** ✅ נסגר 31.7.2026 — ר' "הפתרון" בתחתית

---

## הפתרון (31.7.2026) — TL;DR

שני תיקונים ב-[evidenceMatchService.js](../src/services/evidenceMatchService.js),
שניהם **הידוק** ואף אחד מהם אינו הזזת סף:

1. **`anchorCount` סופר מושגים ולא טוקנים.** הרחבת התחיליות מייצרת שני מונחים
   לאותה מילה (`העבודה` + `עבודה`), ושניהם נמצאים באותו קטע — כך שקטע שנגע
   במושג **אחד** נראה כאילו עמד בדרישת "שני עוגנים מבחינים" של שכבת הנפילה.
   זה בדיוק מה שהכניס את הררי/מרקס/סמית: `anchors=[["עבודה"],["העבודה","עבודה"],…]`.
2. **תנאי-מקדים לשכבת הנפילה: `corpusCoversQuestion`.** הנפילה נפתחת רק אם קיים
   בקורפוס לפחות קטע אחד שנוגע ב**שני** מושגים שונים מהשאלה — הסימן לכך שהקורפוס
   באמת עוסק בצירוף שנשאל. בבקרה השלילית אין אף קטע כזה (המושג הראשי, "בינה
   מלאכותית", נעדר מהקורפוס לגמרי), ולכן הסעיף חוזר `gap`.

**מה נמדד:** `run-scaffold-e2e` → exit 0, 6/6 מחייבים, שלוש הבקרות השליליות
ב-gap, תרחיש 5 (המינהל הציבורי — הסעיף שבשבילו נבנתה שכבת הנפילה) ללא שינוי.
`npm run bench:nlg` → ירוק; שכבת היכולת **זהה בית-בית** לריצת הבסיס באותה מכונה
ובאותו זרע: media-law-2026 88 (87/91/87), mill-2026 82 (84/77/84), אותם סעיפים
חסומים ואותה ספירת מילים. כלומר התיקון חסם רק את הפוזיטיב-השקרי.

⚠️ **על השוואת ציונים:** אותה תצורה נתנה 92/87 בבוקר ו-88/82 אחר הצהריים **עם
אותו קוד** — הקורפוס עצמו זז (קבצי OneDrive שלא ירדו, `⚠ לא ירד מ-OneDrive`).
להשוואת שינוי-קוד צריך ריצת בסיס באותה שעה, לא את ההיסטוריה.

### מה נבדק ונפסל בדרך

- **קרבת השאילתה לצנטרואיד הקורפוס (`qFit`)** — לא מפריד: `human-rights-1948`
  (חיובי) 0.874 מול `neg-ai-labor` 0.888.
- **תדירות-מסמכים של העוגן (`anchorDf`)** — תלוי-קורפוס: העוגנים הלגיטימיים של
  מקרה המינהל הציבורי יושבים על 0.19–0.47, גבוה **פי חמישה** מ-`עבודה` (0.098)
  בבקרה השלילית. סף גלובלי היה הורג את המקרה הלגיטימי.
- **כיסוי מושגי-השאלה בקורפוס (`groupCoverage`)** — לא מפריד: `neg-ai-labor`
  0.67 מול סעיפים אמיתיים של הבנצ' ב-0.63.

---

## התסמין

`npm run bench:nlg` יוצא **exit 1**. שכבת היכולת ירוקה לגמרי — `media-law-2026`
94 (רף 85), `mill-2026` 89 (רף 55). מה שמפיל את הריצה הוא בדיקה **מחייבת** אחת
בשכבת הרגרסיה, בתרחיש 1:

```
✗ neg-ai-labor  [semantic]
     0.870/z3.18  נח הררי – קפיטליזם
     0.850/z3.18  מארקס ואנגלס – המניפסט הקומוניסטי
     0.823/z2.82  אדם סמית
     0.802/z2.80  אדם סמית
─── 6/8 עוברים (6 מחייבים)
```

`requiredOk=5` מול `required=6` ⇒ `process.exit(1)`
([scaffold-e2e.mjs:780](../tools/test-bench/scaffold-e2e.mjs:780)).

## מה הבדיקה אמורה לעשות

[scaffold-e2e.mjs:310](../tools/test-bench/scaffold-e2e.mjs:310) — בקרה שלילית.
השאילתה היא "השפעת הבינה המלאכותית על שוק העבודה" עם מילות מפתח
`בינה מלאכותית · שוק העבודה · אוטומציה`. הקורפוס הוא חומרי קורס על תרבות המערב
(לוק, מיל, מרקס, סמית, חוקות והכרזות) — **אין בו שום דבר על AI**.

`expectGap: true` ⇒ התוצאה הנכונה היא `res.gap`, כלומר "לא נמצאו ראיות".
בפועל האחזור מחזיר ארבעה קטעים מעל רצפת ה-z.

## מה זה כן ומה זה לא

**זו לא נסיגה מהתיקון של `36789b4`.** אותו קומיט נגע בשני מקומות בלבד — פתרון
נתיב הקורפוס ודילוג על קבצי OneDrive שלא ירדו. הוא לא נגע באחזור, בסיפים ולא
בדירוג.

**זה גם לא ידוע-מראש.** שתי בדיקות בתרחיש 1 מסומנות `knownLimit` ולכן אינן
מחייבות; `neg-ai-labor` אינה אחת מהן.

**מה כן ייתכן:** הבדיקה הזאת פשוט מעולם לא רצה על הקורפוס הזה. עד 27.7 ה-`case.json`
הצביעו על `C:/Users/rotem_7jta/...`, כך שכל מדידה היסטורית נעשתה על הקורפוס של
המכונה החזקה. הבדל בתוכן התיקייה בין שתי המכונות מסביר את הפער בלי שום שינוי קוד.

## למה זה מטריד גם אם זו רק דליפה מקומית

הקטעים שחזרו הם כלכלה כללית — הררי על קפיטליזם, המניפסט, סמית. השאילתה מזכירה
"שוק העבודה" ו"כלכלת חליפין", והמנוע מוצא קרבה סמנטית לחומר כלכלי מהמאה ה-19.
z של 2.8–3.2 הוא **מעל** רצפת המקור העברי (3.8 ל-`cleanDigital`, 4.5 אחרת) —
אז ייתכן שהמיון בפועל תלוי באיזו רצפה נבחרה לקטעים האלה. שווה לוודא שהרצפה
שהופעלה כאן היא זו שהתכוונו אליה.

זה בדיוק הכשל שהמנוע נבנה למנוע: סעיף בלי כיסוי אמיתי אמור להיחסם, לא להתמלא
בראיות "קרובות מספיק".

## מה לבדוק במכונה החזקה

1. **להריץ `npm run bench:nlg` שם.** אם `neg-ai-labor` עובר — הפער הוא בתוכן
   הקורפוס, לא במנוע. אז להשוות: אילו קבצים יש בתיקייה אחת ולא בשנייה.
2. אם הוא **נכשל גם שם** — זו חולשת אחזור אמיתית, וההיסטוריה הירוקה נובעת מכך
   שהשכבה הזו לא באמת רצה. אז לבדוק:
   - איזו רצפת z הוחלה על הקטעים האלה בפועל (`cleanDigital` או לא)
   - האם `mad` נמוך מדי בקורפוס הזה ומנפח z (בריצה כאן `mad=0.0195`, `corpus=933`)
   - האם צריך שער נוסף מעבר ל-z — למשל חפיפת ישויות מהשאילתה, כמו האינווריאנטה
     `answers-the-question` שכבר קיימת בשכבת היכולת
3. ⚠️ **לא לתקן בהרפיית סף.** להעלות את הרצפה עד ש-`neg-ai-labor` עובר יחסום
   גם סעיפים לגיטימיים. אם הפתרון הוא סף — צריך למדוד מה זה עושה לשמונה הבדיקות
   האחרות ולשני ה-cases.

## שחזור

```bash
node tools/test-bench/run-scaffold-e2e.mjs
```

הקורפוס נפתר אוטומטית ל-`~/OneDrive/שולחן העבודה/314999533` (40 PDF בשורש,
934 קטעים אחרי סקירה). להצבעה ידנית: `WORDAI_BENCH_CORPUS=<dir>`.

## הקשר

- [tools/test-bench/nlg-bench/README.md](../tools/test-bench/nlg-bench/README.md) — עיצוב הבנצ' הדו-שכבתי
- [docs/nlg-handoff.md](nlg-handoff.md) — מצב המנוע המקומי
- `36789b4` — תיקון הנתיבים שחשף את הבדיקה הזאת
